// js/audio/audio.js
//
// Owns: mic recording, audio file import, non-destructive trim
// (trimStart/trimEnd on project.audioClips[0]), Web Audio API
// playback of just the trimmed slice, synced against the timeline.
//
// Not built yet — step 5 of the build order.

export function initAudio(/* project */) {
  // TODO: record/import, trim UI, Web Audio playback
}
