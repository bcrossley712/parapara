// js/timeline/timeline.js
//
// Owns: frame array/order, thumbnails, reorder logic, playback loop,
// transport controls state (play/pause/fps).
//
// Reads/writes the shared project.frames array — does not reach into
// canvas/ or storage/ internals directly.
//
// Not built yet — step 4 of the build order (after drawing engine +
// data model).

export function initTimeline(/* project */) {
  // TODO: frame strip, playback loop, onion-skin toggle wiring
}
